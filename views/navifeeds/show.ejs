<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>

    <div class="navifeed navifeed-show">
      <h2>Show</h2>
      <div>
        <h3><%= navifeed.content%></h3>
        <!-- <label>Email</label><span><%= navifeed.email %></span>
        <br>
        <label>Phone</label><span><%= navifeed.phone %></span> -->
      </div>
      <div class="navifeed-menu">
        <a href="/navifeeds/<%= navifeed._id %>/edit">Edit</a>
        <form action="/navifeeds/<%= navifeed._id %>?_method=delete" method="post">
          <a href="#" onclick="confirm('Do you want to delete this?')?this.parentElement.submit():null;">Delete</a>
        </form>
      </div>
    </div>
  </body>
</html>
